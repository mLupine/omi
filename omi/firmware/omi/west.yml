# West manifest for production legacy build
manifest:
  self:
    path: omi

  projects:
    # This is a minimal west.yml for legacy production builds
    # The actual nRF Connect SDK workspace is set up in CI